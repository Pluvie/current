test(arena_init_provided_capacity) {

  //given("an arena")
  //  struct arena* = arena_init();
  //
  //when("the provided capacity is greater or equal than the minimum")
  //
  //calling("arena_init()")
  //
  //must("allocate the arena with the provided capacity")
  //  verify(arena.capacity == 16);

  //success()
  //  map_free(&map);
}
